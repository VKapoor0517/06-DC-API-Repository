spring:
  datasource:
    password: postgres
    url: jdbc:postgresql://localhost:5432/TESTDB
    username: postgres
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect